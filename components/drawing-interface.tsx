"use client"

// This is a simplified version that doesn't do anything
// We're keeping it to avoid breaking imports, but our new interface will be used instead
export function DrawingInterface() {
  return null
}

